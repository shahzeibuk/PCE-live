/** Hero backgrounds in `public/` — art-directed per breakpoint in `HeroCurrencyBackdrop`. */

/** PKR notes / press — reads well on narrow viewports */
export const CURRENCY_HERO_BACKGROUND_MOBILE = '/W6qgc.jpg'

/** USD, PKR & market chart — wider scene for tablet/desktop heroes */
export const CURRENCY_HERO_BACKGROUND_DESKTOP = '/wLlhs.jpg'

/**
 * Default single asset for blocks that use one image (note surfaces, CMS hero fallback).
 */
export const CURRENCY_HERO_BACKGROUND_IMAGE = CURRENCY_HERO_BACKGROUND_DESKTOP
