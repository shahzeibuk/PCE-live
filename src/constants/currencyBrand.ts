/** Re-export hero assets for any legacy imports. Prefer `@/utilities/heroBackgrounds`. */
export {
  HERO_BACKGROUND_PATHS,
  pickRandomHeroBackground,
  pickRandomHeroBackgroundPair,
  type HeroBackgroundPath,
} from '@/utilities/heroBackgrounds'
